

services:
  langtranslator-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped


  # langtranslator-demo:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.streamlit
  #   ports:
  #     - "8501:8501"
  #   env_file:
  #     - .env
  #   restart: unless-stopped

volumes:
  data:
